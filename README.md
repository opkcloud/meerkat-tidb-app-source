# meerkat-tidb-app
```agsl
1. 通过 mvn clean install -DskipTests 打包后端工程代码，生成 ***.jar 包；
2. 执行 java -jar ***.jar，启动后端工程；
```
```agsl
1. Package the back-end project code through 'mvn clean install -DskipTests' to generate ***.jar package;
2. Execute 'java -jar ***.jar' to start the backend project;
```
